declare type DateFormatter = 'number' | 'string' | false;
/**
 * 这里主要是来转化一下数据
 * 将 moment 转化为 string
 * 将 all 默认删除
 * @param value
 * @param dateFormatter
 * @param proColumnsMap
 */
declare const conversionSubmitValue: <T = any>(value: T, dateFormatter: DateFormatter, valueTypeMap: {
    [key: string]: string;
}) => T;
export default conversionSubmitValue;
